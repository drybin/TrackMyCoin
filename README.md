# TrackMyCoin

Track my coin - CLI инструмент для работы с Google Sheets

Started at: 29.12.2025

## Возможности

- 📊 Чтение данных из Google Sheets
- 🔐 Поддержка Service Account аутентификации
- 🔑 Поддержка API Key аутентификации
- 💰 Автоматическое получение цен с CoinGecko API
- 🏗️ Структурированное хранение данных о ценах монет
- ✅ Валидация и парсинг данных

## Быстрый старт

### 1. Установка зависимостей

```bash
go mod tidy
```

### 2. Настройка аутентификации

**Вариант A: Service Account (рекомендуется)**

1. Поместите ваш `service-account-file.json` в корень проекта
2. Создайте файл `.env`:

```env
GOOGLE_SERVICE_ACCOUNT_FILE=service-account-file.json
GOOGLE_SHEET_ID=your_spreadsheet_id
GOOGLE_SHEET_RANGE=
```

**Примечание:** `GOOGLE_SHEET_RANGE` можно оставить пустым - программа автоматически прочитает первый лист.

3. Дайте доступ к документу email из service account файла (поле `client_email`)

**Вариант B: API Key (только для публичных документов)**

Создайте файл `.env`:

```env
GOOGLE_API_KEY=your_api_key
GOOGLE_SERVICE_ACCOUNT_FILE=
GOOGLE_SHEET_ID=your_spreadsheet_id
GOOGLE_SHEET_RANGE=
```

**Документация:**
- ⚡ Быстрая справка: [QUICK_REFERENCE.md](./QUICK_REFERENCE.md)
- 🚀 Руководство пользователя: [USAGE_GUIDE.md](./USAGE_GUIDE.md)
- 🪙 Правила названий монет: [COIN_NAMING.md](./COIN_NAMING.md)
- 🔧 Настройка Google Sheets: [GOOGLE_SHEETS_SETUP.md](./GOOGLE_SHEETS_SETUP.md)
- 📊 Логика заполнения цен: [PRICE_FILLING_LOGIC.md](./PRICE_FILLING_LOGIC.md)

### 3. Формат данных в таблице

⚠️ **ВАЖНО:** В колонке "Монета" указывайте **только символ** монеты!

```
✅ Правильно:  XVG, BTC, ETH, DOGE
❌ Неправильно: XVGUSDT, BTC/USDT, verge
```

Подробнее: [COIN_NAMING.md](./COIN_NAMING.md)

### 4. Запуск

```bash
# Вывести Hello World
go run ./cmd/cli/... hello-world

# Прочитать данные из Google Sheets и заполнить цены
go run ./cmd/cli/... process
```

## Доступные команды

- `hello-world` - Вывести Hello World
- `process` - Прочитать данные из Google Sheets и обработать:
  - Парсинг строк в структуры `CoinPriceRecord`
  - Автоматическое заполнение пустых цен Bybit через CoinGecko API
  - Вывод подробной статистики

## Как работает команда `process`

1. **Подключение и чтение данных**
   - Подключается к Google Sheets и читает данные
   - Показывает информацию о таблице (название, список листов)

2. **Парсинг данных**
   - Парсит каждую строку в структуру `CoinPriceRecord` с полями:
     - Дата, Время (в часовом поясе GMT+7), Источник, Монета, Направление
     - Цена в источнике, Цена на Bybit
     - Цены через различные временные интервалы (10 мин, 30 мин, 1 час, 2 часа, 6 часов, 12 часов, 24 часа, 3 дня, 5 дней, 7 дней, 1 месяц)

3. **Умное заполнение пропущенных цен**
   - Для каждой записи проверяет все пустые поля с ценами
   - Для `BybitPrice`: если пустая, получает текущую цену
   - Для временных полей (Price10Min, Price30Min, и т.д.):
     - Вычисляет время, когда должна быть эта цена (исходное время + интервал)
     - Если это время уже наступило и поле пустое → получает текущую цену с CoinGecko
     - Если время еще не наступило → пропускает (цену еще рано получать)
   
   **Пример:** Запись создана в 10:00. Текущее время 10:35:
   - ✅ `Price10Min` (нужна цена на 10:10) - время прошло, заполняем
   - ✅ `Price30Min` (нужна цена на 10:30) - время прошло, заполняем  
   - ❌ `Price1Hour` (нужна цена на 11:00) - еще не наступило, пропускаем

4. **Автоматическое обновление Google Sheets**
   - После заполнения цен все данные автоматически записываются обратно в таблицу
   - Обновляются только строки с данными (заголовки остаются нетронутыми)
   - Пустые цены остаются пустыми в таблице

5. **Статистика**
   - Выводит подробную информацию по каждой записи
   - Общая статистика: сколько цен заполнено, сколько ошибок
   - Подтверждение успешной записи в Google Sheets

## Поддерживаемые монеты

CoinGecko API поддерживает тысячи криптовалют. Вот некоторые из популярных:

**Основные:** BTC, ETH, USDT, BNB, SOL, XRP, USDC, ADA, AVAX, DOGE  
**DeFi:** UNI, LINK, AAVE, SUSHI, CRV, LDO  
**Layer 2:** MATIC, ARB, OP  
**Новые:** SUI, SEI, TIA, APT, INJ

И многие другие! Полный список доступен на [CoinGecko](https://www.coingecko.com/).

## Структура проекта

```
.
├── cmd/cli/              # Точка входа в приложение
├── internal/
│   ├── adapter/webapi/   # Адаптеры для внешних API
│   │   ├── google_sheets.go  # Google Sheets API
│   │   └── coingecko.go      # CoinGecko API
│   ├── app/cli/          # Бизнес-логика CLI
│   │   ├── config/       # Конфигурация
│   │   ├── registry/     # DI контейнер
│   │   └── usecase/      # Use cases
│   ├── domain/model/     # Доменные модели
│   │   └── coin_price_record.go  # Модель записи о цене
│   └── presentation/     # Команды CLI
├── pkg/                  # Общие утилиты
└── .env                  # Конфигурация (создайте самостоятельно)
```